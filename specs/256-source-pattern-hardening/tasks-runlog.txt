# F-2 (256) Source-Pattern Hardening — Build Run Log

This file accumulates per-task numerical evidence for Wave 1..Wave 11 of the
Source-Pattern Hardening feature build. Each section is appended in order; do
not edit prior sections. Sections are referenced from ADR-040 §Consequences and
the Day-5/Day-8 slip-watch checkpoints.

## T004: NFR-002 Dependency Manifest Baseline (Date: 2026-05-05)

Captured at branch `256-source-pattern-hardening`, HEAD `5879074` (commit
"chore(256): checkpoint before build resume"). T050 will re-snapshot at
end-of-build and assert the diff against this baseline is empty.

Manifests present at repo root:
- `pyproject.toml` — Python project metadata (dev deps live in
  requirements-dev.txt)
- `requirements-dev.txt` — pinned Python dev/test dependencies
- `package.json` — NOT PRESENT (verified absent at repo root); no Node runtime
  dependencies in this template-only repo

SHA-256 baselines (`git show HEAD:<file> | shasum -a 256`):

2c0b511fc7e86d093b2d586e6052edc39138a51b99b4cf473c5dd56839efb555  pyproject.toml
e41f5e052f9b6edffd8489ce30044e443f9516ae6ecc270e6b37403ee220cf98  requirements-dev.txt

NFR-002 verification at T050 MUST re-compute these two hashes against
end-of-build HEAD and assert byte-identical equality. If `package.json` is
introduced by F-2 work, that itself is a NFR-002 violation.

## T005: Stream 1 Day-1 Baseline Benchmark (Pre-Refactor) (Date: 2026-05-05)

Methodology per SC-010: 100 invocations × 4 fixtures × p50/p95; per-file delta
(NOT aggregate); warm-cache + cold-cache reported separately. Each invocation
forks a fresh `bash -c 'source <fixture>'` so timings include process-fork +
bash-startup + source-parse + return — i.e., the end-to-end cost an adopter
incurs at /aod.update or init.sh time. Two warm-up invocations precede each
fixture's 100-run timing window to amortize first-run JIT/cache effects.

Environment:
- Host: Darwin 25.3.0 (macOS)
- Bash (local): GNU bash 5.3.9(1)-release (aarch64-apple-darwin25.1.0)
- Note: T011/T012 will run identical numbers on macOS bash 3.2.57 in CI
  (Day-5 slip-watch GREEN-LIGHT condition 1) — local 5.3.9 timings here
  represent steady-state developer-loop numbers. The relative per-file delta
  vs T013 post-refactor is what matters; absolute numbers will shift modestly
  on bash 3.2 but the comparison axis is held constant.

Fixtures benchmarked:
- Site A (init.sh:106) → stacks/nextjs-supabase/defaults.env
- Site A (init.sh:106) → stacks/fastapi-react/defaults.env
- Site B (template-git.sh:561) → tests/fixtures/config-load/valid/aod-kit-version-valid
- Site D (template-substitute.sh:209) → tests/fixtures/config-load/valid/personalization-env-valid

Note: Site C (template-substitute.sh:217/249/536/558) is `eval` removal — not
a `source` site — so no T005 baseline applies. T005 covers source-file load
sites only; Site C's measurement is "eval removed" (binary), not timing.

WARM CACHE (steady-state, post page-cache priming):

| Fixture                              | Site                          | p50 (ms) | p95 (ms) | mean (ms) | min (ms) | max (ms) | σ (ms) |
|--------------------------------------|-------------------------------|----------|----------|-----------|----------|----------|--------|
| stacks/nextjs-supabase/defaults.env  | A (init.sh:106)               |    8.892 |   18.938 |    10.378 |    5.658 |   27.994 |  4.531 |
| stacks/fastapi-react/defaults.env    | A (init.sh:106)               |   10.040 |   21.753 |    11.324 |    5.520 |   28.447 |  5.373 |
| aod-kit-version-valid (fixture)      | B (template-git.sh:561)       |   10.224 |   24.135 |    12.264 |    5.852 |   32.179 |  6.149 |
| personalization-env-valid (fixture)  | D (template-substitute.sh:209)|   10.408 |   21.458 |    12.241 |    6.023 |   40.185 |  5.780 |

All four fixtures cluster within ~1.5ms p50 of each other (8.9-10.4ms),
indicating the dominant cost is bash-process-startup (~5-6ms baseline)
amortized over a small parse pass — file-content size at ≤300 bytes is below
the noise floor of process-fork variance. This means NFR-004's 25% threshold
(≤2.5ms p50 increase) will be tight; the post-refactor library MUST keep
per-call cost bounded by ~2-3ms over the existing source path to stay green.

COLD CACHE: N/A
- `sudo purge` (macOS) requires elevated privileges that are not available in
  this autonomous build session. Cold-cache measurement is deferred to T013
  (post-refactor benchmark) where the same constraint will apply equally to
  both pre- and post-refactor numbers; the per-file delta remains meaningful
  even with warm-cache-only data because both sides of the comparison sit on
  the same OS cache state. Warm-cache numbers above represent the steady-state
  path that adopters experience after a single config-load operation primes
  the page cache (the dominant case for /aod.update + init.sh runs).

NFR-004 escalation thresholds (applied in T013 against these baselines):
- ≤5% delta: proceed; no PRD update.
- 5-25% delta: proceed; loosen NFR-004 to 25% in ADR-040 §Consequences.
- 25-50% delta: Team-Lead approval required + security tradeoff rationale in ADR-040.
- >50% delta: escalate to PM for re-scope (Q-5 Option c levers).

These numbers will be folded into ADR-040 §Consequences at T054 (final).

## T011: Library Verification on macOS bash 3.2.57 (Date: 2026-05-05)

Bash binary: /bin/bash (GNU bash 3.2.57(1)-release, arm64-apple-darwin25)
Pytest: 8.4.2 via `python3 -m pytest`
Test file: tests/scripts/test_template_config_load_unit.py
Library under test: .aod/scripts/bash/template-config-load.sh

Command (canonical): BASH=/bin/bash python3 -m pytest tests/scripts/test_template_config_load_unit.py::test_template_config_load -v

Result: 29/29 cases pass (100%).

Test surface (parametrized id breakdown):
- case_0_canary_positive — passes (FR-007 module-load canary; pipe-subshell trap detector)
- case_1..5 — valid KV no-whitelist (5 value shapes); all pass
- case_6 — valid 5-key whitelist; passes
- case_7..10 — adversarial (cmd-substitution, unbalanced quote, backtick, $); all reject exit 8
- case_11 — lowercase key in upper mode rejected exit 8
- case_12 — missing whitelisted key rejected exit 8
- case_13 — extra (disallowed) key rejected exit 8
- case_14 — KEY without `=` rejected exit 8
- case_15 — embedded NUL rejected exit 8 (Step 2b NUL pre-check via tr-d-size-comparison)
- case_16 — bare KEY= empty unquoted PASS (B-1)
- case_17 — invalid var_prefix "1bad-prefix" rejected exit 1 (H-1 defensive check)
- case_18 — empty double-quoted value PASS (B-1 reinforcement)
- case_19..23 — B-3 cases: trailing-newline / no-trailing-newline / CRLF / leading-whitespace / blank-line-then-content; all pass
- case_24 — empty <path> rejected exit 1
- case_25 — non-existent <path> rejected exit 3
- case_26a — <key_case>=lower accepts lowercase
- case_26b — <key_case>=lower rejects uppercase exit 8
- case_27 — <key_case>=mixed rejected exit 1 (Q-2.5)

Note: the 27-case contract test surface is rendered as 29 parametrized
entries because case 26 is split into 26a/26b for clarity (separate
positive + negative assertions on the same lower-mode behavior).

One implementation note worth surfacing during architect re-review at
T054: the original Step 5 contract said "the regex implicitly excludes
embedded newlines (the read already split lines), and embedded NUL
(bash regex stops at NUL)". The latter is incorrect — bash command
substitution silently truncates content at the first NUL byte, so the
regex never sees the NUL at all. Without explicit NUL detection, the
adversarial fixture `KEY=foo<NUL>bar\n` would be loaded as `KEY=foobar\n`
(one valid KV line), bypassing the rejection promise in FR-005 AC-5.4.

The library implements an explicit Step 2b NUL pre-check that compares
`wc -c` against `tr -d '\000' | wc -c` — any difference signals NUL
presence and rejects with exit 8 + stderr "NUL byte". This idiom is
bash 3.2 + BSD-coreutils compatible; it does not rely on `grep -q $'\x00'`
which is unreliable because bash collapses embedded NULs in argv to
empty strings (matching everything).

This Step 2b is a small but material elaboration of the FR-005 AC-5.4
"the regex implicitly rejects newline / NUL" wording. It does not
break the contract (the contract guarantees rejection; the Step 2b is
the mechanism that delivers the rejection). T054 should fold this
detail into ADR-040 §Decision Item 1 + §Consequences as a clarification
to the contract.

Day-5 slip-watch GREEN-LIGHT condition 1 status: GREEN (29/29 ≥ 17/17).

## T012: Library Verification on Linux bash 4+ — DEFERRED TO CI (Date: 2026-05-05)

The autonomous build environment is macOS-only; Linux verification will
run automatically via the existing CI matrix (.github/workflows/) on the
next push to the 256-source-pattern-hardening branch.

Cross-checked that the library uses no bash 4-only features:
- No associative arrays (`declare -A`)
- No `mapfile` / `readarray`
- No `${var,,}` / `${var^^}` lowercase/uppercase parameter expansion
- No `&>` redirection (only `>&2` and `2>/dev/null`)
- All array access uses indexed-array syntax (`${arr[i]}` and `${arr[@]}`)
- One internal `eval` carve-out for indirect array access (per ADR-040
  Decision Item 7); no other eval anywhere in the file

Bash 3.2 → bash 4+ portability is by construction. As a sanity check,
I also ran the same 29-case suite under /opt/homebrew/bin/bash (5.3.9)
on the local machine; all 29 cases passed identically. This is
informally suggestive that Linux bash 5.x in CI will pass too, but the
authoritative answer arrives with the next CI run.

## T013: Stream 1 Day-1 Post-Implementation Benchmark (Date: 2026-05-05)

Methodology per SC-010 (matches T005 baseline exactly):
- 100 invocations × 4 fixtures × p50/p95
- Per-file delta (NOT aggregate)
- Warm-cache (cold-cache deferred to CI per T005 limitation)
- Two warm-up invocations precede each fixture's 100-run timing window
- Each invocation forks `bash -c 'source <library>; aod_template_load_kv_file ...'`
  so timings include process-fork + bash-startup + library-source +
  function-execution + return — i.e., the realistic per-call cost an
  adopter incurs at /aod.update or init.sh time

Environment:
- Host: Darwin 25.3.0 (macOS)
- Bash (local): /opt/homebrew/bin/bash 5.3.9 (matches T005 — same binary)
- Note: T005 baseline numbers were captured under the same Homebrew bash
  5.3.9, so the per-file delta comparison is on the same axis.

Fixtures (matching T005 baseline exactly):
- Site A → stacks/nextjs-supabase/defaults.env (4-key whitelist applied
  per actual file content; the file ships 4 of 5 canonical keys —
  TECH_STACK_DATABASE/VECTOR/AUTH + CLOUD_PROVIDER. T016 will need a
  follow-on update to either add TECH_STACK to defaults.env or relax
  the whitelist to 4 keys; this is a Site A site-specific concern out
  of scope for T013 measurement.)
- Site A → stacks/fastapi-react/defaults.env (same 4-key whitelist)
- Site B → tests/fixtures/config-load/valid/aod-kit-version-valid (no
  whitelist; lower mode)
- Site D → tests/fixtures/config-load/valid/personalization-env-valid
  (12-key AOD_CANONICAL_PLACEHOLDERS whitelist; AOD_PERSONALIZATION_
  prefix)

WARM CACHE (steady-state, post page-cache priming):

| Fixture                              | Site                          | p50 (ms) | p95 (ms) | mean (ms) | min (ms) | max (ms) | σ (ms) |
|--------------------------------------|-------------------------------|----------|----------|-----------|----------|----------|--------|
| stacks/nextjs-supabase/defaults.env  | A (init.sh:106)               |   30.919 |   63.848 |    33.703 |   14.475 |   80.284 | 15.414 |
| stacks/fastapi-react/defaults.env    | A (init.sh:106)               |   27.972 |   56.730 |    32.092 |   14.593 |   94.383 | 14.604 |
| aod-kit-version-valid (fixture)      | B (template-git.sh:561)       |   36.839 |   74.082 |    38.300 |   14.837 |   84.943 | 17.486 |
| personalization-env-valid (fixture)  | D (template-substitute.sh:209) |   33.016 |   60.023 |    35.137 |   16.702 |   80.212 | 14.728 |

PER-FILE DELTA vs T005 BASELINE:

| Fixture                              | T005 p50 | T013 p50 | Δ p50 % | T005 p95 | T013 p95 | Δ p95 % |
|--------------------------------------|----------|----------|---------|----------|----------|---------|
| stacks/nextjs-supabase/defaults.env  |    8.892 |   30.919 |  +247.7 |   18.938 |   63.848 |  +237.2 |
| stacks/fastapi-react/defaults.env    |   10.040 |   27.972 |  +178.6 |   21.753 |   56.730 |  +160.8 |
| aod-kit-version-valid (fixture)      |   10.224 |   36.839 |  +260.3 |   24.135 |   74.082 |  +207.0 |
| personalization-env-valid (fixture)  |   10.408 |   33.016 |  +217.2 |   21.458 |   60.023 |  +179.7 |

NFR-004 escalation tier: **>50% — STOP and escalate to PM for re-scope**.

Absolute cost framing (for PM/Architect re-scope discussion):
- T005 baseline: 9-10ms p50 per invocation (`bash -c 'source <fixture>'`)
- T013 post-impl: 28-37ms p50 per invocation (`bash -c 'source <library>;
  aod_template_load_kv_file ...'`)
- Absolute increase: ~20-27ms per call
- For init.sh (one invocation per adopter setup): 20-27ms is invisible
  to humans (adopter perception threshold ~100ms).
- For /aod.update (one invocation per weekly run): same.
- For automated test loops (e.g., 100 unit-test invocations of the
  function): additive ~20ms × 100 = 2 seconds — perceptible in CI but
  small.

Cost decomposition (mini-investigation):
- `bash -c 'source <library-only>'`: ~13ms p50 (library file is 200
  lines of bash with regex compilation and function definition)
- `bash -c 'source <fixture-only>'`: ~10ms p50 (T005 baseline shape)
- Function execution itself (minus library source): ~17-25ms (regex
  pass + whitelist scan + printf -v assignments)

The dominant cost driver is the library-source overhead (which does NOT
exist in the T005 baseline because T005's "library" is implicitly the
fixture file itself). In production, the library is sourced ONCE per
init.sh run — the per-call cost in real adopter usage is much closer to
T005's baseline + the function-only ~15ms increment.

Recommendation for PM/Architect re-scope decision:
1. The benchmark methodology (fresh `bash -c` per call) AMORTIZES
   library-source cost over 1 call, which inflates the relative delta.
2. In real usage, the library is sourced once and the function is called
   multiple times — actual per-call cost converges toward T005 baseline
   + ~15ms (still in the 5-25% tier on p95 absolute terms, well above
   25% on p50).
3. Absolute per-call cost remains <50ms in worst case — still well
   below adopter perception threshold.
4. Q-5 Option a (accept-and-document, loosen NFR-004 to 50% with
   security tradeoff rationale) is the natural disposition; the
   methodology asymmetry is the substantive observation.

Proposed disposition (pending Architect/Team-Lead/PM concurrence):
- ADR-040 §Consequences documents the methodology asymmetry +
  amortized-vs-per-call distinction explicitly.
- NFR-004 threshold remains formally at 25% but ADR-040 records the
  one-time-per-process cost as the dominant driver.
- T054 finalizes ADR-040 §Consequences with these numbers.

Day-5 slip-watch impact:
- This finding does NOT affect Day-5 GREEN-LIGHT conditions 1-4 (test
  pass count, Site A green, ADR-040 Proposed, watchdog smoke-test).
- Performance disposition is a Day-8/Day-10 ADR consideration, not a
  Day-5 blocking concern.

These numbers will be folded into ADR-040 §Consequences at T054 (final
Architect/Team-Lead approval recorded in spec.md per Q-5 Option a).

## PM Disposition: Q-5 Option a (Accept-and-Document) — APPROVED 2026-05-05

**Decision authority**: PM (user, davidmatousek@me.com)
**Trigger**: T013 NFR-004 >50% tier (per-file p50 deltas +178% to +260%; absolute cost ~30-37ms p50)
**Disposition**: Q-5 Option a — accept-and-document with ADR-040 §Consequences elaborations to be folded in at T054.

**Required ADR-040 elaborations (T054)**:
1. Document the methodology asymmetry — the per-call benchmark fork-execs `bash -c` per invocation, attributing one-time-per-process library-source overhead (~13ms) to each call. Real adopter usage sources the library ONCE per script run; marginal per-call cost in production converges toward T005 baseline + ~15ms function execution.
2. Loosen NFR-004 to permit the >50% delta with security tradeoff rationale (architecturally the library is the right place to be; per-process source cost is the dominant component; absolute cost ~30-37ms is sub-perception and well below the ~100ms human threshold).
3. Record the cost decomposition: library-source ~13ms + function execution ~15-25ms = ~28-37ms post-impl per fork-exec.
4. Maintain awk micro-opt path REJECTED (Q-5 ruling) — BSD vs GNU awk variance + audit-readability concerns dominate the marginal performance gain.

**Collateral findings to fold into ADR-040 (T054)**:
- §Decision Item 1 clarification: the original "regex implicitly rejects NUL" mechanism is unsound (bash command substitution silently truncates at the first NUL). The library uses an explicit Step 2b NUL pre-check via `wc -c` vs `tr -d '\000' | wc -c` size comparison. Behavior unchanged (NUL → exit 8); only the mechanism description needs updating.

**Day-5 slip-watch impact**: NONE. Performance disposition is a Day-8/Day-10 ADR consideration (T054), not a Day-5 blocking concern. Condition 1 is GREEN at 29/29 unit tests pass on macOS bash 3.2.57.

**Wave 3 unblocked**: proceed.


## Wave 3 Site A: defaults.env refactor (T014-T019, Date: 2026-05-05)

**Architect E1 prerequisite executed**: Option α — `TECH_STACK="nextjs"` and
`TECH_STACK="fastapi-react"` added to the two shipped `defaults.env` files.
Canonical 5-key whitelist preserved in `STACK_PACK_ALLOWED_KEYS`.

**Files modified**:
- `stacks/nextjs-supabase/defaults.env` — added `TECH_STACK="nextjs"` (canonical 5-key surface)
- `stacks/fastapi-react/defaults.env` — added `TECH_STACK="fastapi-react"` (canonical 5-key surface)
- `scripts/init.sh` — sourced `template-config-load.sh`; replaced `source defaults.env`
  with `aod_template_load_kv_file ... STACK_PACK_ALLOWED_KEYS`; mapped
  `STACK_TECH_STACK` etc. into the canonical-12 placeholder vars expected
  downstream.

**Files added**:
- `tests/scripts/test_init_sh_defaults_env.py` — 4 test cases (2 stack-pack
  positive paths × {nextjs-supabase, fastapi-react} + 2 adversarial cases)
- `tests/fixtures/config-load/adversarial/malicious-pack-defaults.env` — L-2
  header carried; `CUSTOM_HOOK="$(touch /tmp/F-256-pwned)"` payload.
- `tests/fixtures/config-load/adversarial/missing-key-pack-defaults.env` — L-2
  header carried; CLOUD_PROVIDER deliberately omitted.

**Verification**: macOS bash 3.2.57 — all 4 cases pass after Site A commit
land in HEAD. Pre-commit dry-run had 2/4 fail due to test using HEAD clone;
post-commit verification (T019) is gated on Site A landing on the branch.

**Linux verification**: deferred to CI per T012 pattern.

**TACHI-VULN-6f5a95085056 (HIGH) closed**: malicious pack rejected exit 8 + no
/tmp/F-256-pwned artifact — verified by integration test pattern.

## Wave 3 Site B: aod-kit-version refactor (T020-T024, Date: 2026-05-05)

**Files modified**:
- `.aod/scripts/bash/template-git.sh`:
  - Top-level: defensive source of `template-config-load.sh` (post-F-2
    dependency).
  - `aod_template_read_version_file:561` — `source "$path" || ...` replaced
    with `aod_template_load_kv_file "$path" "" "" lower`. Per-field validators
    at `:568+` run unchanged.
  - `aod_template_write_version_file:485-515` (line :501) — `source "$tmp_path"
    2>/dev/null` replaced with `aod_template_load_kv_file "$tmp_path" "" "" lower
    2>/dev/null` inside the existing subshell. Round-trip preserved.

**Files added**:
- `tests/scripts/test_template_config_load_integration.py` (Site B section) —
  4 cases: malformed (command-injection rejected); valid lowercase 5-field;
  bare `version=` empty (B-1 contract); UPPERCASE rejected in lowercase mode.
  Also Site B-roundtrip H-3 case: writer round-trip via library succeeds.
- `tests/fixtures/config-load/adversarial/aod-kit-version-malformed` — L-2
  header carried; `; touch /tmp/F-256-pwned` injection payload.

**Verification**: macOS bash 3.2.57 — all 5 Site B cases (4 + roundtrip) pass.

**TACHI-VULN-bf5496e9fcdf (HIGH) closed**: command-injection rejected exit 8 +
/tmp/F-256-pwned never created (verified in test fixture).

## Wave 3 Site C: eval removal + writer escape pass removal + F-1 amendment (T025-T033, Date: 2026-05-05)

**STRICT REORDER executed per Architect E2/E3**: T032 (init-input.sh validator
amendment) landed BEFORE T031 (writer escape pass removal).

**Files modified**:
- `.aod/scripts/bash/template-substitute.sh`:
  - `:217` (T027) — `eval "val=\"\${$key:-}\""` → `local var_name="$key"; val="${!var_name:-}"`
  - `:249` (T028) — `eval "AOD_PERSONALIZATION_${key}=\"\$val\""` → `printf -v "AOD_PERSONALIZATION_${key}" '%s' "$val"`
  - `:536` (T029) — `eval "val=\"\${$key:-}\""` → `var_name="$key"; val="${!var_name:-}"`
  - `:558` (T030) — `eval "val=\"\${$key}\""` → `var_name="$key"; val="${!var_name}"` (NO `:-` default; H-4 strict semantic)
  - `:566-571` (T031) — four-line escape block REMOVED; replaced with direct
    `printf '%s="%s"\n' "$key" "$val"` emission. Safe because T032 rejects
    `$`, `\`, backtick at the prompt boundary upstream.
  - Top-level: defensive source of `template-config-load.sh`.
- `.aod/scripts/bash/init-input.sh:171+` (T032) — F-1 contract amendment:
  rejection ladder extended with `if [[ "$answer" =~ [\$\\\`] ]]; then
  reason="metachar (\$, \\, backtick) not allowed"`. Inserted between
  cntrl-class check and over-length check (more-specific-class-first
  ordering preserved).

**Files added**:
- `tests/scripts/test_template_substitute_lint_no_eval.py` — FR-007 lint:
  `grep -c '\beval\b' template-substitute.sh` returns 0. All commit comments
  rephrased to reference "ADR-040 Decision Item 7" rather than the bare
  token, so the lint catches both executable invocations and comment
  re-introduction.

**Files modified for tests**:
- `tests/scripts/test_init_input_unit.py` (T026) — augmented with 3 new
  parametrize cases: case_13_metachar_dollar, case_14_metachar_backslash,
  case_15_metachar_backtick. All 8 cases (5 pre-existing + 3 new) pass.

**Verification**: macOS bash 3.2.57:
- `grep -c '\beval\b' template-substitute.sh` → 0 (asserted)
- Lint test passes (1 case)
- F-1 init-input test (8 cases including 3 metachar) passes
- F-1 substitute unit test (8 cases) regression-free

**TACHI-VULN-9a7512071b4a (MEDIUM) closed**: zero eval invocations remain;
F-1 contract amendment lands as B-2 Path R-2 ripple.

## Wave 3 Site D: TOCTOU collapse (T034-T036, Date: 2026-05-05)

**Files modified**:
- `.aod/scripts/bash/template-substitute.sh:162-209` (T035) — REPLACED 47-line
  body of `aod_template_load_personalization_env` with ~7 lines of library
  delegation:
  ```
  local path="${1:-}"
  if [ -z "$path" ]; then ...; return 1; fi
  aod_template_load_kv_file "$path" "AOD_PERSONALIZATION_" AOD_CANONICAL_PLACEHOLDERS
  ```
  Behavior preserved per FR-005: missing-path → 1; file-absent → 3;
  validation-failure → 8 (newline / NUL / metachar via Step 5 + Step 2b
  pre-check); missing-key → 8 (Step 6 post-pass via whitelist); caller-scope
  AOD_PERSONALIZATION_<KEY> populated (Step 7 printf -v).

**Files added**:
- Site D section in `tests/scripts/test_template_config_load_integration.py`
  — 5 + 1 cases: collapsed-body PASS (4 canonical placeholders verified in
  caller scope); missing-path → 1; file-absent → 3; embedded-NUL → 8 with
  "NUL byte" stderr; missing-canonical-key → 8 with "CLOUD_PROVIDER" stderr;
  TOCTOU-residual race test (bash 3.2-portable `&` + `sleep 0.005` + `mv`
  pattern).

**Verification**: macOS bash 3.2.57 — all 6 Site D cases pass.

**Linux verification**: deferred to CI.

**TACHI-VULN-4dc6cf8f88ea (MEDIUM) closed**: TOCTOU race window collapsed to
single `cat` (per H-2 framing); race-test asserts caller-scope value matches
ONE snapshot (initial OR swapped, never partial).

## Wave 3 Stream 4: clone timeout watchdog (T037-T041, Date: 2026-05-05)

**Files modified**:
- `.aod/scripts/bash/template-git.sh:68-150` — `aod_template_fetch_upstream`
  wrapped with bash 3.2-portable background+kill watchdog per FR-006 + L-1:
  - Validate `AOD_FETCH_TIMEOUT` against regex `^[1-9][0-9]*$` (default 60).
    Reject `=0` (Q-3 footgun), `=abc` (non-numeric), `=01` (leading zero).
  - Background `git clone &`; capture clone_pid.
  - Spawn `( sleep $TIMEOUT && kill -TERM $clone_pid ) </dev/null >/dev/null 2>&1 &`;
    capture watchdog_pid. The fd redirection is REQUIRED — without it, the
    orphaned `sleep` child keeps the caller's stdout/stderr pipe open,
    blocking subprocess.run's EOF detection (verified on macOS bash 3.2.57
    + Python 3.9 subprocess).
  - Install L-1 trap `INT TERM EXIT` that kills the watchdog on outer
    interrupt; cleared on every return path.
  - `wait $clone_pid; clone_rc=$?`.
  - Cleanup post-wait: `kill -9 $watchdog_pid 2>/dev/null; wait $watchdog_pid
    2>/dev/null` (SIGKILL because bash 3.2 SIGTERM-of-subshell may not
    propagate to sleep child reliably).
  - On `clone_rc 143` (SIGTERM) or `130` (SIGINT) → `rm -rf $destdir`,
    timeout error message, `trap - INT TERM EXIT`, return 9.
  - On any other rc → propagate (return rc) and clear trap.

**Files added**:
- `tests/scripts/test_template_git_clone_timeout.py` — 6 cases:
  - case_1_timeout_3s: hanging fixture + AOD_FETCH_TIMEOUT=3 → exit 9 in ~3-4s
  - case_2_timeout_10s: hanging fixture + AOD_FETCH_TIMEOUT=10 → exit 9 at ~10s
  - case_3_zero_footgun: AOD_FETCH_TIMEOUT=0 → exit 1 (Q-3)
  - case_4_non_numeric: AOD_FETCH_TIMEOUT=abc → exit 1
  - case_5_leading_zero: AOD_FETCH_TIMEOUT=01 → exit 1
  - case_6_fast_clone_succeeds: synthetic upstream + AOD_FETCH_TIMEOUT=60
    → exit 0 + no zombie watchdog (elapsed <10s confirms no fd leak)
- `tests/scripts/conftest.py` (modified): `hanging_upstream` session-scoped
  pytest fixture per F-250 ADR-039 fixture-scope canon. `socket.bind(0)`
  ephemeral port + listen + accept-loop daemon thread that holds connections
  open without responding.

**T040 macOS bash 3.2.57 smoke test** (Day-5 GREEN-LIGHT condition 4):
- Hanging clone with timeout=3 → exit 9 at 3.0-3.4s ✓
- Hanging clone with timeout=10 → exit 9 at 10.0-10.5s ✓
- Fast clone with timeout=60 → exit 0 in <2s + no zombie watchdog ✓
- Invalid AOD_FETCH_TIMEOUT (=0, =abc, =01) → exit 1 + named footgun message ✓

All 6/6 cases pass on `BASH=/bin/bash` (3.2.57). Linux verification deferred
to CI per T012 pattern.

**TACHI-VULN-851fd6a21ba9 (LOW) closed**: clone timeout enforced; L-1
trap eliminates watchdog process-leak window.

**Day-5 slip-watch GREEN-LIGHT condition 4: GREEN.**
