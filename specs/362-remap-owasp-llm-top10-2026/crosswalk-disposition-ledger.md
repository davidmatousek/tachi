# Crosswalk Disposition Ledger (F-362)

Scaffolded at **T002** per [contracts/disposition-ledgers.md](contracts/disposition-ledgers.md) and [data-model.md](data-model.md) §4. One row per LLM-keyed edge in `schemas/taxonomy/crosswalk.yaml`, in the file's native edge order. The mechanical σ-permutation (T006) and the citation URL-scheme gate (T003) are diff-reviewable elsewhere; this ledger records the **human** disposition — the part no test can check.

**Completion bar** (target, per contract): 74/74 dispositioned · 57/57 σ-applied · 54/54 citation-actions resolved (re-anchored or interim per plan D9).
**Current status**: 74/74 dispositioned · 57/57 σ-applied (T006 ✓) · 54/54 + 7 M-3 citation-actions re-anchored to interim-resource-page (61 total) + 13/13 unchanged non-OWASP mitre = 74/74 citation-actions resolved — T007 human disposition complete 2026-08-09 (target-verdict: 74 valid-2026 / 0 revised / 0 dropped; confidence-action: 71 hold / 3 downgrade — rows 29, 40, 72; full per-edge reasoning in `.aod/results/security-analyst.md`). Column ownership (P1 checkpoint M-1 correction, 2026-08-09): `re-key` was filled by **T006** (mechanical σ record); `target-verdict` / `confidence-action` / `citation-action` / `reviewer-note` are filled by **T007** after the **T003** URL-policy gate (closed: Branch 2).

---

### URL policy (T003)

**Verdict**: BRANCH 2 (interim-resource-page) — Verified 2026-08-09

**Anchor Policy**: Per-entry OWASP LLM Top 10 2026 pages do NOT exist on genai.owasp.org. All attempted 2026-specific URL patterns returned 404 (tested: `/llmrisk/llm012026-prompt-injection/`, `/llmrisk/llm082026-hidden-context-exposure/`, `/llm-top-10-2026/`, and variants). Evidence: 8 fetch attempts, 6 unique 404s, 2 successful confirmations of 2025 editions still live. The released 2026 Top 10 document (published 2026-08-04) is currently only available as a PDF download from the release resource page.

**Citation Anchor** (all re-anchored citations per T007):
```
https://genai.owasp.org/resource/owasp-genai-llm-top-10-2026/
```

**Application**: 54 crosswalk edges with year-slugged (2025-edition) OWASP LLM citations will be re-anchored to the interim resource page (per data-model §4). When per-entry 2026 pages appear and are verified, T007 will update ledger rows and apply the new URLs in bulk. Non-year-slugged URLs (20 edges) remain candidates for unchanged status pending T007 review. See full fetch evidence in `.aod/results/web-researcher.md`.

---

### σ-oracle preconditions (T002)

**Method**: live Python script (stdlib + PyYAML, no new dependency — rung 3/5 of the laziness ladder) against `schemas/taxonomy/crosswalk.yaml` at HEAD `601d7d4` (branch `362-remap-owasp-llm-top10-2026`), run 2026-08-09. `yaml.safe_load()` over the full edge list (a flat list of 5-key edge dicts — no nested traversal needed); counts computed by exact key match on `source.taxonomy`, `source.id` (regex `^LLM(0[1-9]|10)$`), `target.taxonomy`, `target.id`, `edge_type`, and substring `"2025"` in `citation`. This is a direct structural read, not a `grep` approximation, so it cannot be fooled by formatting (e.g., the four edges whose `target.taxonomy` is `mitre-attack`/`mitre-atlas` but whose `citation` still anchors to a year-slugged `genai.owasp.org` URL are correctly counted because the check reads each edge's own `citation` field, not an inference from its target).

| # | Check | Expected | Live result | Verdict |
|---|---|---|---|---|
| 1 | Total crosswalk edges | 645 | 645 | MATCH |
| 2 | LLM-keyed edges (`source.taxonomy == owasp`, `source.id` ∈ LLM01–LLM10) | 74 (66 primary / 8 related) | 74 (66 primary / 8 related) | MATCH |
| 3 | LLM-keyed edges as a **target** endpoint (any source, `target.taxonomy == owasp`, `target.id` ∈ LLM01–LLM10) | 0 | 0 | MATCH |

**Header status: CLEAN — no count divergence.** All three σ-oracle counts match the data-model §3 / plan MEDIUM-2 projections exactly.

Check 3 is the **σ-oracle precondition for T006** (architect plan MEDIUM-2): source-only permutation is only complete if zero crosswalk edges carry an OWASP-LLM id as a target endpoint. Verified 0 at `601d7d4` — T006 may proceed with a source-only single-pass σ-permutation. T006 re-verifies this count immediately before applying the permutation (an LLM target endpoint added between this measurement and T006's run would be invisible to both the byte-identity integrity suite and this source-keyed ledger).

**Citation year-slug pre-note** (mechanical URL inspection, see Ledger below): of the 74 LLM-keyed edges, **54 carry a year-slugged (2025-edition) citation URL** needing re-anchor, and 20 carry a non-year URL (`unchanged-candidate`). 54 matches the plan D9 / data-model §4 completion-bar denominator exactly.

**Independent cross-check** (not one of the three required counts, included for confidence): per-`source.id` 2025 edge counts are LLM01=8, LLM02=9, LLM03=8, LLM04=6, LLM05=8, LLM06=7, LLM07=6, LLM08=7, LLM09=4, LLM10=11 (Σ=74). Permuting these through σ (data-model §1: LLM03→LLM04, LLM04→LLM05, LLM05→LLM10, LLM06→LLM03, LLM07→LLM08, LLM08→LLM09, LLM09→LLM07, LLM10→LLM06, LLM01/LLM02 held) reproduces the post-σ per-2026-id projection in data-model §3 (8/9/7/8/6/11/4/6/7/8) exactly, an independent confirmation that the live file matches the plan's counting.

---

## Ledger

Columns per data-model §4. Only `#` and `edge` are pre-seeded from the live file. `re-key`, `target-verdict`, and `confidence-action` are left `—` (pending T006/T007). `citation-action` also reads `—` (no disposition made yet) but carries a **mechanical** parenthetical pre-note — year-slugged vs non-year URL, from live URL inspection only, **not** a disposition — so T007 can triage without re-deriving it; the actual `re-anchored: <2026 URL>` / `interim-resource-page` / `unchanged: <URL>` verdict is T007's, applied after the T003 gate. `reviewer-note` is free text, left `—`.

| # | edge | re-key | target-verdict | confidence-action | citation-action | reviewer-note |
|---|------|--------|-----------------|--------------------|--------------------|-----------------|
| 1 | LLM01 → cwe:CWE-77 (primary) | held | valid-2026 | hold | interim-resource-page | M-3: llm01-prompt-injection/ is confirmed still-2025-edition content (T003 evidence) despite lacking a year token; re-anchored for consistency with the interim policy applied to the other 66 edges rather than silently inheriting the unchanged-candidate pre-note. CWE-77 (Command Injection) is a modality-agnostic consequence of successful injection in tool-calling agents; textual or cross-modal, the injection→exec chain is identical. |
| 2 | LLM02 → cwe:CWE-200 (primary) | held | valid-2026 | hold | interim-resource-page | — |
| 3 | LLM03 → cwe:CWE-1395 (primary) | σ-applied: LLM03→LLM04 | valid-2026 | hold | interim-resource-page | — |
| 4 | LLM04 → cwe:CWE-20 (primary) | σ-applied: LLM04→LLM05 | valid-2026 | hold | interim-resource-page | Root-cause link: unvalidated ingestion of external training/fine-tuning data is the base mechanism poisoning exploits; 2026's +fine-tuning-subversion delta still ingests via the same unvalidated path. |
| 5 | LLM05 → cwe:CWE-79 (primary) | σ-applied: LLM05→LLM10 | valid-2026 | hold | interim-resource-page | — |
| 6 | LLM01 → cwe:CWE-20 (primary) | held | valid-2026 | hold | interim-resource-page | M-3: llm01-prompt-injection/ is confirmed still-2025-edition content (T003 evidence) despite lacking a year token; re-anchored for consistency with the interim policy applied to the other 66 edges rather than silently inheriting the unchanged-candidate pre-note. Improper Input Validation is injection's textbook root cause, regardless of input modality. |
| 7 | LLM01 → cwe:CWE-94 (primary) | held | valid-2026 | hold | interim-resource-page | M-3: llm01-prompt-injection/ is confirmed still-2025-edition content (T003 evidence) despite lacking a year token; re-anchored for consistency with the interim policy applied to the other 66 edges rather than silently inheriting the unchanged-candidate pre-note. Code Injection via prompt-injected tool/code-interpreter abuse; modality-agnostic. |
| 8 | LLM01 → cwe:CWE-1427 (primary) | held | valid-2026 | hold | interim-resource-page | M-3: llm01-prompt-injection/ is confirmed still-2025-edition content (T003 evidence) despite lacking a year token; re-anchored for consistency with the interim policy applied to the other 66 edges rather than silently inheriting the unchanged-candidate pre-note. CWE-1427 is CWE's dedicated LLM-prompt-injection weakness — direct 1:1 fit regardless of modality; 2026's cross-modal addition doesn't disturb it. |
| 9 | LLM02 → cwe:CWE-201 (primary) | held | valid-2026 | hold | interim-resource-page | — |
| 10 | LLM02 → cwe:CWE-538 (primary) | held | valid-2026 | hold | interim-resource-page | — |
| 11 | LLM02 → cwe:CWE-359 (primary) | held | valid-2026 | hold | interim-resource-page | — |
| 12 | LLM03 → cwe:CWE-1357 (primary) | σ-applied: LLM03→LLM04 | valid-2026 | hold | interim-resource-page | — |
| 13 | LLM03 → cwe:CWE-494 (primary) | σ-applied: LLM03→LLM04 | valid-2026 | hold | interim-resource-page | 2026's +model-artifact-authenticity delta directly strengthens this mapping: unverified-integrity downloads are the exact mechanism the new scope calls out. |
| 14 | LLM03 → cwe:CWE-829 (primary) | σ-applied: LLM03→LLM04 | valid-2026 | hold | interim-resource-page | — |
| 15 | LLM04 → cwe:CWE-502 (primary) | σ-applied: LLM04→LLM05 | valid-2026 | hold | interim-resource-page | Insecure deserialization is the mechanistic delivery path for tampered/backdoored model checkpoints (e.g. malicious pickle payloads) — an established poisoning vector distinct from LLM04's acquisition-integrity framing. |
| 16 | LLM04 → cwe:CWE-915 (primary) | σ-applied: LLM04→LLM05 | valid-2026 | hold | interim-resource-page | Mass-assignment-style attribute tampering is a plausible vector for corrupting training/fine-tuning configuration objects; medium (unchanged) reflects the indirect fit. |
| 17 | LLM04 → cwe:CWE-1395 (primary) | σ-applied: LLM04→LLM05 | valid-2026 | hold | interim-resource-page | Overlaps row 3's LLM04 mapping of the same CWE-1395: LLM04 frames it as acquisition-integrity (Supply Chain), this edge frames it as a poisoning-vector (compromised/vulnerable pretrained-weight source) — both are legitimate angles on the same weakness; medium correctly subordinate to row 3's high. |
| 18 | LLM05 → cwe:CWE-89 (primary) | σ-applied: LLM05→LLM10 | valid-2026 | hold | interim-resource-page | — |
| 19 | LLM05 → cwe:CWE-116 (primary) | σ-applied: LLM05→LLM10 | valid-2026 | hold | interim-resource-page | — |
| 20 | LLM05 → cwe:CWE-78 (primary) | σ-applied: LLM05→LLM10 | valid-2026 | hold | interim-resource-page | — |
| 21 | LLM05 → cwe:CWE-601 (primary) | σ-applied: LLM05→LLM10 | valid-2026 | hold | interim-resource-page | — |
| 22 | LLM06 → cwe:CWE-250 (primary) | σ-applied: LLM06→LLM03 | valid-2026 | hold | interim-resource-page | — |
| 23 | LLM06 → cwe:CWE-269 (primary) | σ-applied: LLM06→LLM03 | valid-2026 | hold | interim-resource-page | — |
| 24 | LLM06 → cwe:CWE-285 (primary) | σ-applied: LLM06→LLM03 | valid-2026 | hold | interim-resource-page | — |
| 25 | LLM06 → cwe:CWE-732 (primary) | σ-applied: LLM06→LLM03 | valid-2026 | hold | interim-resource-page | — |
| 26 | LLM07 → cwe:CWE-200 (primary) | σ-applied: LLM07→LLM08 | valid-2026 | hold | interim-resource-page | ACUTE (LLM08 broadened-scope review): CWE-200 is a generic exposure CWE that scales cleanly from the narrower 2025 System-Prompt-Leakage framing to the broader 2026 Hidden-Context-Exposure framing (system prompt, tool defs, retrieved memory, internal state) — strengthened, not weakened, by the rename. |
| 27 | LLM07 → cwe:CWE-538 (primary) | σ-applied: LLM07→LLM08 | valid-2026 | hold | interim-resource-page | ACUTE: file/dir exposure of hidden context (any kind, not just the system prompt) remains squarely in scope under the broadened category. |
| 28 | LLM07 → cwe:CWE-540 (primary) | σ-applied: LLM07→LLM08 | valid-2026 | hold | interim-resource-page | ACUTE: hardcoded hidden-context artifacts (prompts, tool configs) embedded in source remain in scope under the broadened category. |
| 29 | LLM07 → cwe:CWE-1426 (primary) | σ-applied: LLM07→LLM08 | valid-2026 | downgrade: CWE-1426 is GenAI-output-validation-specific and only indirectly captures hidden-context confidentiality; cross-check shows this is the crosswalk's one confidence inversion vs. its more-natural, already-medium LLM07 sibling (row 35) | interim-resource-page | ACUTE (LLM08): CWE-1426 'Improper Validation of Generative AI Output' governs GenAI output-validation generally and only indirectly captures Hidden Context Exposure — specifically the sub-channel where leaked context escapes through ungated model output, not direct extraction, logs, or source (already covered by sibling edges 26-28). Cross-check confirms an inversion: this crosswalk consistently subordinates a CWE's non-native category use to medium beneath its native-category high (see CWE-116, CWE-1395, CWE-79, CWE-94 pairs) — CWE-1426's more natural home is LLM07 Misinformation (row 35, already medium), so LLM08's use is the derivative one. Downgrading high→medium to correct the inversion. |
| 30 | LLM08 → cwe:CWE-200 (primary) | σ-applied: LLM08→LLM09 | valid-2026 | hold | interim-resource-page | — |
| 31 | LLM08 → cwe:CWE-209 (primary) | σ-applied: LLM08→LLM09 | valid-2026 | hold | interim-resource-page | — |
| 32 | LLM08 → cwe:CWE-285 (primary) | σ-applied: LLM08→LLM09 | valid-2026 | hold | interim-resource-page | Multi-tenant/cross-context vector-store authorization gaps are a documented RAG sub-risk. |
| 33 | LLM08 → cwe:CWE-639 (primary) | σ-applied: LLM08→LLM09 | valid-2026 | hold | interim-resource-page | IDOR-style vector/document-id access without ownership check. |
| 34 | LLM09 → cwe:CWE-345 (primary) | σ-applied: LLM09→LLM07 | valid-2026 | hold | interim-resource-page | — |
| 35 | LLM09 → cwe:CWE-1426 (primary) | σ-applied: LLM09→LLM07 | valid-2026 | hold | interim-resource-page | This is CWE-1426's most natural home in the LLM catalog — ungrounded/unvalidated GenAI output reaching users as fact is the definition of Misinformation; cross-check against row 29's weaker LLM08 use of the same CWE. |
| 36 | LLM09 → cwe:CWE-20 (primary) | σ-applied: LLM09→LLM07 | valid-2026 | hold | interim-resource-page | — |
| 37 | LLM10 → cwe:CWE-400 (primary) | σ-applied: LLM10→LLM06 | valid-2026 | hold | interim-resource-page | — |
| 38 | LLM10 → cwe:CWE-770 (primary) | σ-applied: LLM10→LLM06 | valid-2026 | hold | interim-resource-page | — |
| 39 | LLM10 → cwe:CWE-799 (primary) | σ-applied: LLM10→LLM06 | valid-2026 | hold | interim-resource-page | — |
| 40 | LLM10 → cwe:CWE-918 (primary) | σ-applied: LLM10→LLM06 | valid-2026 | downgrade: SSRF's dominant real-world impact is credential/metadata exfiltration (tachi-info-disclosure's documented remit, not this category's denial-of-service/model-theft agents) — the Denial-of-Wallet framing here is real but secondary | interim-resource-page | SSRF's dominant real-world impact is credential/metadata exfiltration (an information-disclosure concern — the tachi-info-disclosure agent's registered scope explicitly owns 'SSRF to cloud metadata', not tachi-denial-of-service or tachi-model-theft, this category's two citation-attested agents). The cost-amplification/Denial-of-Wallet framing used here is real but secondary. Downgrading medium→low. |
| 41 | LLM01 → mitre-atlas:AML.T0051 (primary) | held | valid-2026 | hold | unchanged: https://atlas.mitre.org/techniques/AML.T0051 | ATLAS AML.T0051 'LLM Prompt Injection' — canonical, name-identical fit; technique description is modality-agnostic so the 2026 cross-modal addition doesn't narrow it. |
| 42 | LLM04 → mitre-atlas:AML.T0020 (primary) | σ-applied: LLM04→LLM05 | valid-2026 | hold | unchanged: https://atlas.mitre.org/techniques/AML.T0020 | ATLAS AML.T0020 'Poison Training Data' — canonical, direct fit. |
| 43 | LLM04 → mitre-atlas:AML.T0018 (primary) | σ-applied: LLM04→LLM05 | valid-2026 | hold | unchanged: https://atlas.mitre.org/techniques/AML.T0018 | ATLAS AML.T0018 'Manipulate AI Model' — direct fit (model manipulation = model poisoning). |
| 44 | LLM03 → mitre-atlas:AML.T0010 (primary) | σ-applied: LLM03→LLM04 | valid-2026 | hold | unchanged: https://atlas.mitre.org/techniques/AML.T0010 | ATLAS AML.T0010 'AI Supply Chain Compromise' — canonical, name-identical fit. |
| 45 | LLM03 → mitre-attack:T1195 (primary) | σ-applied: LLM03→LLM04 | valid-2026 | hold | interim-resource-page | ATT&CK T1195 'Supply Chain Compromise' — canonical, name-identical fit (verified via attack.mitre.org). |
| 46 | LLM06 → mitre-atlas:AML.T0061 (primary) | σ-applied: LLM06→LLM03 | valid-2026 | hold | unchanged: https://atlas.mitre.org/techniques/AML.T0061 | ATLAS AML.T0061 'LLM Prompt Self-Replication' — injection-delivered but agency-consequence framing (autonomous propagation without human direction); matches this category's own catalog citation sub-pattern 'Instruction Hijack (Per-Request)' under tachi-tool-abuse. Not mis-filed — the worm-like autonomous-propagation behavior is the excessive-agency angle, injection is only the delivery mechanism. |
| 47 | LLM06 → mitre-attack:T1548 (primary) | σ-applied: LLM06→LLM03 | valid-2026 | hold | unchanged: https://attack.mitre.org/techniques/T1548/ | ATT&CK T1548 'Abuse Elevation Control Mechanism' — direct, strong fit for the permissions/privilege sub-theme. |
| 48 | LLM07 → mitre-atlas:AML.T0057 (primary) | σ-applied: LLM07→LLM08 | valid-2026 | hold | unchanged: https://atlas.mitre.org/techniques/AML.T0057 | ACUTE (LLM08): ATLAS AML.T0057 'LLM Data Leakage' ('prompts that induce the LLM to leak sensitive information') is a direct, edition-agnostic fit — broadened scope doesn't narrow it. |
| 49 | LLM10 → mitre-attack:T1499 (primary) | σ-applied: LLM10→LLM06 | valid-2026 | hold | unchanged: https://attack.mitre.org/techniques/T1499/ | ATT&CK T1499 'Endpoint Denial of Service' — canonical fit. |
| 50 | LLM10 → mitre-attack:T1498 (primary) | σ-applied: LLM10→LLM06 | valid-2026 | hold | unchanged: https://attack.mitre.org/techniques/T1498/ | ATT&CK T1498 'Network Denial of Service' — canonical fit. |
| 51 | LLM05 → mitre-atlas:AML.T0051 (primary) | σ-applied: LLM05→LLM10 | valid-2026 | hold | unchanged: https://atlas.mitre.org/techniques/AML.T0051 | ATLAS AML.T0051 'LLM Prompt Injection' — indirect: canonical primary home is LLM01 (row 41); held at the same uniform medium the crosswalk applies to every AML.T0051 appearance (no inversion vs. siblings). Reflects injection-as-precursor to unsanitized generated content reaching a downstream sink. |
| 52 | LLM05 → mitre-atlas:AML.T0060 (primary) | σ-applied: LLM05→LLM10 | valid-2026 | hold | unchanged: https://atlas.mitre.org/techniques/AML.T0060 | ATLAS AML.T0060 'Publish Hallucinated Entities' — 2026's +insecure-generated-code-at-scale delta directly strengthens this mapping: attacker-registered resources matching LLM-hallucinated names (e.g. dependency/package slopsquatting) is a canonical instance of unvalidated generated output being trusted downstream. |
| 53 | LLM08 → mitre-atlas:AML.T0057 (primary) | σ-applied: LLM08→LLM09 | valid-2026 | hold | unchanged: https://atlas.mitre.org/techniques/AML.T0057 | ATLAS AML.T0057 'LLM Data Leakage' — distinct framing from row 48's LLM08 use of the same technique: this edge captures prompt-induced leakage of RAG/vector-retrieved content specifically, not system-prompt/hidden-context leakage. |
| 54 | LLM08 → mitre-atlas:AML.T0051 (primary) | σ-applied: LLM08→LLM09 | valid-2026 | hold | unchanged: https://atlas.mitre.org/techniques/AML.T0051 | ATLAS AML.T0051 'LLM Prompt Injection' — indirect: injection is a documented delivery mechanism for RAG/vector-store poisoning (indirect prompt injection via a poisoned corpus); canonical primary home remains LLM01 (row 41), held at the same uniform medium the crosswalk applies to every AML.T0051 appearance (no inversion vs. siblings, so not singled out for downgrade). |
| 55 | LLM01 → cwe:CWE-79 (primary) | held | valid-2026 | hold | interim-resource-page | M-3: llm01-prompt-injection/ is confirmed still-2025-edition content (T003 evidence) despite lacking a year token; re-anchored for consistency with the interim policy applied to the other 66 edges rather than silently inheriting the unchanged-candidate pre-note. Correctly subordinate (medium) beneath the native LLM10 high-confidence CWE-79 mapping (row 5) — XSS surfaces via unsanitized *output* even when the root cause is injected input. |
| 56 | LLM01 → cwe:CWE-116 (primary) | held | valid-2026 | hold | interim-resource-page | M-3: llm01-prompt-injection/ is confirmed still-2025-edition content (T003 evidence) despite lacking a year token; re-anchored for consistency with the interim policy applied to the other 66 edges rather than silently inheriting the unchanged-candidate pre-note. Correctly subordinate beneath the native LLM10 high-confidence CWE-116 mapping (row 19). |
| 57 | LLM02 → cwe:CWE-532 (primary) | held | valid-2026 | hold | interim-resource-page | — |
| 58 | LLM02 → cwe:CWE-209 (primary) | held | valid-2026 | hold | interim-resource-page | — |
| 59 | LLM05 → cwe:CWE-94 (primary) | σ-applied: LLM05→LLM10 | valid-2026 | hold | interim-resource-page | Correctly subordinate (medium) beneath the native LLM01 high-confidence CWE-94 mapping (row 7). |
| 60 | LLM06 → cwe:CWE-284 (primary) | σ-applied: LLM06→LLM03 | valid-2026 | hold | interim-resource-page | — |
| 61 | LLM08 → cwe:CWE-287 (primary) | σ-applied: LLM08→LLM09 | valid-2026 | hold | interim-resource-page | Generic infra-security angle: unauthenticated vector-store/embedding-API endpoints allowing unauthorized retrieval. |
| 62 | LLM10 → cwe:CWE-1333 (primary) | σ-applied: LLM10→LLM06 | valid-2026 | hold | interim-resource-page | — |
| 63 | LLM02 → mitre-attack:T1005 (primary) | held | valid-2026 | hold | interim-resource-page | ATT&CK T1005 'Data from Local System' (Collection tactic) — local-system data harvesting fits disclosure directly. |
| 64 | LLM02 → mitre-attack:T1213 (primary) | held | valid-2026 | hold | interim-resource-page | ATT&CK T1213 'Data from Information Repositories' (Collection tactic) — repository data harvesting fits disclosure directly. |
| 65 | LLM03 → mitre-attack:T1195.001 (primary) | σ-applied: LLM03→LLM04 | valid-2026 | hold | interim-resource-page | ATT&CK T1195.001 'Compromise Software Dependencies and Development Tools' — direct fit. |
| 66 | LLM03 → mitre-attack:T1195.002 (primary) | σ-applied: LLM03→LLM04 | valid-2026 | hold | interim-resource-page | ATT&CK T1195.002 'Compromise Software Supply Chain' — direct, near-exact-name fit. |
| 67 | LLM01 → mitre-atlas:AML.T0054 (related) | held | valid-2026 | hold | interim-resource-page | M-3: llm01-prompt-injection/ is confirmed still-2025-edition content (T003 evidence) despite lacking a year token; re-anchored for consistency with the interim policy applied to the other 66 edges rather than silently inheriting the unchanged-candidate pre-note. ATLAS AML.T0054 'LLM Jailbreak' correctly typed 'related' (a common injection-adjacent technique, not itself prompt injection). Citation carried a '(Related Frameworks AML.TXXXX)' provenance suffix documented in schemas/taxonomy/README.md §4.1 (F-182 OWASP-LLM→ATLAS class: medium ceiling because the citation is an inferred one-hop from the source page's own 'Related Frameworks' list, not a hard ID cross-ref). The interim resource page has no per-category Related Frameworks section, so re-anchoring orphans that evidentiary chain until real 2026 per-entry pages exist and are re-verified — flagged as a follow-up, suffix dropped from the citation text per the T003 'EXACTLY <url>' instruction. |
| 68 | LLM02 → mitre-atlas:AML.T0024 (related) | held | valid-2026 | hold | interim-resource-page | ATLAS AML.T0024 'Exfiltration via AI Inference API' — API-based exfil of sensitive data, direct fit. Citation carried a '(Related Frameworks AML.TXXXX)' provenance suffix documented in schemas/taxonomy/README.md §4.1 (F-182 OWASP-LLM→ATLAS class: medium ceiling because the citation is an inferred one-hop from the source page's own 'Related Frameworks' list, not a hard ID cross-ref). The interim resource page has no per-category Related Frameworks section, so re-anchoring orphans that evidentiary chain until real 2026 per-entry pages exist and are re-verified — flagged as a follow-up, suffix dropped from the citation text per the T003 'EXACTLY <url>' instruction. |
| 69 | LLM07 → mitre-atlas:AML.T0051 (related) | σ-applied: LLM07→LLM08 | valid-2026 | hold | interim-resource-page | ACUTE (LLM08): correctly typed 'related' — injection is a common delivery vector for context extraction, not itself an exposure technique; broadened 2026 scope doesn't change this relationship. Citation carried a '(Related Frameworks AML.TXXXX)' provenance suffix documented in schemas/taxonomy/README.md §4.1 (F-182 OWASP-LLM→ATLAS class: medium ceiling because the citation is an inferred one-hop from the source page's own 'Related Frameworks' list, not a hard ID cross-ref). The interim resource page has no per-category Related Frameworks section, so re-anchoring orphans that evidentiary chain until real 2026 per-entry pages exist and are re-verified — flagged as a follow-up, suffix dropped from the citation text per the T003 'EXACTLY <url>' instruction. |
| 70 | LLM09 → mitre-atlas:AML.T0048 (related) | σ-applied: LLM09→LLM07 | valid-2026 | hold | interim-resource-page | ATLAS AML.T0048 'External Harms' — generic consequence framing (abusing a manipulated AI system to harm external parties, e.g. disinformation victims); correctly typed 'related', appropriately conservative. |
| 71 | LLM10 → mitre-atlas:AML.T0024 (related) | σ-applied: LLM10→LLM06 | valid-2026 | hold | interim-resource-page | ATLAS AML.T0024 'Exfiltration via AI Inference API' — model-extraction via high-volume querying is simultaneously an exfiltration technique and a resource-consumption mechanism; matches this category's dual tachi-model-theft/denial-of-service catalog attestation. Citation carried a '(Related Frameworks AML.TXXXX)' provenance suffix documented in schemas/taxonomy/README.md §4.1 (F-182 OWASP-LLM→ATLAS class: medium ceiling because the citation is an inferred one-hop from the source page's own 'Related Frameworks' list, not a hard ID cross-ref). The interim resource page has no per-category Related Frameworks section, so re-anchoring orphans that evidentiary chain until real 2026 per-entry pages exist and are re-verified — flagged as a follow-up, suffix dropped from the citation text per the T003 'EXACTLY <url>' instruction. |
| 72 | LLM10 → mitre-atlas:AML.T0025 (related) | σ-applied: LLM10→LLM06 | valid-2026 | downgrade: 'Exfiltration via Cyber Means' involves no resource-exhaustion mechanism (unlike row 71's inference-API exfil) — its link to Unbounded Consumption rests only on shared model-theft agent ownership, not a consumption mechanism | interim-resource-page | ATLAS AML.T0025 'Exfiltration via Cyber Means' (generic-cyber-means artifact theft) doesn't itself involve resource exhaustion — unlike row 71's inference-API exfil (a consumption mechanism), this technique's link to Unbounded Consumption rests solely on the category's shared model-theft agent ownership, not a consumption mechanism. Downgrading medium→low. Citation carried a '(Related Frameworks AML.TXXXX)' provenance suffix documented in schemas/taxonomy/README.md §4.1 (F-182 OWASP-LLM→ATLAS class: medium ceiling because the citation is an inferred one-hop from the source page's own 'Related Frameworks' list, not a hard ID cross-ref). The interim resource page has no per-category Related Frameworks section, so re-anchoring orphans that evidentiary chain until real 2026 per-entry pages exist and are re-verified — flagged as a follow-up, suffix dropped from the citation text per the T003 'EXACTLY <url>' instruction. |
| 73 | LLM10 → mitre-atlas:AML.T0029 (related) | σ-applied: LLM10→LLM06 | valid-2026 | hold | interim-resource-page | ATLAS AML.T0029 'Denial of AI Service' — near-canonical availability fit. Citation carried a '(Related Frameworks AML.TXXXX)' provenance suffix documented in schemas/taxonomy/README.md §4.1 (F-182 OWASP-LLM→ATLAS class: medium ceiling because the citation is an inferred one-hop from the source page's own 'Related Frameworks' list, not a hard ID cross-ref). The interim resource page has no per-category Related Frameworks section, so re-anchoring orphans that evidentiary chain until real 2026 per-entry pages exist and are re-verified — flagged as a follow-up, suffix dropped from the citation text per the T003 'EXACTLY <url>' instruction. |
| 74 | LLM10 → mitre-atlas:AML.T0034 (related) | σ-applied: LLM10→LLM06 | valid-2026 | hold | interim-resource-page | ATLAS AML.T0034 'Cost Harvesting' — literally the Denial-of-Wallet mechanism this category's own catalog citation names explicitly. Citation carried a '(Related Frameworks AML.TXXXX)' provenance suffix documented in schemas/taxonomy/README.md §4.1 (F-182 OWASP-LLM→ATLAS class: medium ceiling because the citation is an inferred one-hop from the source page's own 'Related Frameworks' list, not a hard ID cross-ref). The interim resource page has no per-category Related Frameworks section, so re-anchoring orphans that evidentiary chain until real 2026 per-entry pages exist and are re-verified — flagged as a follow-up, suffix dropped from the citation text per the T003 'EXACTLY <url>' instruction. |
