# Session Continuation: F-362 Remap OWASP LLM Top 10 → 2026

**Generated**: 2026-08-09 (Session A, part 1 — /aod.build standalone, 3-wave ceiling reached)
**Branch**: `362-remap-owasp-llm-top10-2026`
**Last Commit**: `28d2f04` docs(362): T003 D9 URL-scheme gate closed — Branch 2 interim-resource-page (Wave A3)
**Draft PR**: #363 (branch pushed through Wave A3)

## Completed This Session

- `601d7d4` — **Wave A1 / T001** pre-state capture: gated 15-module subset GREEN (147P/1S/1xf); taxonomy integrity GREEN (5P); byte-identity suite **pre-existing RED** (6F/7P/1S, environmental font-subset-tag drift `ZZMJWG+` vs `SPUEUJ+`); 19 out-of-gate failures across 7 modules (matches ~19 expectation); #345 xfail present, 0 xpass. Artifact: `test-results-prestate.md`.
- `9ce8311` — **Wave A2 / T002 ∥ T004**: both disposition ledgers scaffolded (crosswalk 74 rows pre-seeded; σ-oracle preconditions verified LIVE: 645 edges / 74 LLM-keyed = 66p+8r / **0 LLM target endpoints** / 54 year-slugged; bare-code ledger with Tier-A lanes T008/T009 + Tier-B skeleton, 366+103 census bars). ADR-048 index entry added (additive-only).
- `9fb98be` — **P0 architect checkpoint: APPROVED_WITH_CONCERNS — GO.** Ruling 1: T004 status line correct-as-applied → T004 closed. Ruling 2: **T024 no-churn evidence form pinned** (see Critical Context below).
- `28d2f04` — **Wave A3 / T003** D9 URL-scheme gate: **Branch 2 (interim-resource-page)**. Per-entry 2026 pages absent (6× 404 incl. renamed-LLM08 slug; llmrisk pages still 2025-content). Anchor = `https://genai.owasp.org/resource/owasp-genai-llm-top-10-2026/`. URL authoring now UNBLOCKED for T005/T007.

## Current State

- **Phase**: implement (Build) — issue #362 at stage:build on the board
- **Tasks**: 4/26 complete (T001–T004 ✓). Waves 1–3 of 14 done (A1, A2, A3).
- **Uncommitted**: sibling-session churn only — see the ⚠️ pre-flight warning below. Nothing of F-362's is uncommitted.

## ⚠️ Pre-Flight Warning (read BEFORE running /aod.build)

The working tree carries ~35 modifications NOT belonging to F-362: 34 PNGs under `examples/{agentic-app,maestro-reference,mermaid-agentic-app}/**/attack-{chains,trees}/` (regenerated by concurrent sibling Claude sessions — verified unrelated to F-362; see the Environmental note in `test-results-prestate.md`) plus auto-regenerated `docs/product/_backlog/BACKLOG.md`.

**`/aod.build` Step 0h auto-commits uncommitted changes as a checkpoint. That MUST NOT sweep the `examples/**` PNGs into this branch** — `examples/**` is the FR-008 carve-out (F-362b); FR-008 AC requires zero protected files modified on the feature-branch diff. Before running /aod.build:
1. `git status --porcelain` — if the `examples/**` PNGs are still dirty and no sibling session is active, ask David how to disposition them (they are another workstream's output — do not silently `git restore` them, do not commit them here). Stashing (`git stash push -- examples/`) is the safe holding move if David is unavailable.
2. `BACKLOG.md` churn is harmless (regenerated view) — commit or leave as convenient.

## Critical Context Carried Forward

1. **P0 Ruling 2 (binding on T024)** — recorded in `test-results-prestate.md` §P0 ruling: no-churn proof = A1 zero baseline bytes changed (git) + A2 failing-node-id set exactly the T001 six / all greens stay green + A3 divergence confined to font-subset-tag class (web-app offset 224224 stable). Baseline regen REJECTED (F-362b owns it); toolchain pinning REJECTED. Environmental-red follow-up issue is MANDATORY → absorbed into T026's issue-filing batch.
2. **URL policy (T003, ledger header)**: interim resource-page anchor for all 54 re-anchors; per-edge recording by T007; deliver-stage link-rot dispatch is the final validator.
3. **σ-oracle preconditions (T002, ledger header)**: 0 LLM target endpoints re-verified → T006's source-only σ is complete. Post-σ per-2026-id count vector must be exactly `8/9/7/8/6/11/4/6/7/8`.
4. **Sequencing law (signed)**: T005 → T006 → T007 strict serial. σ must be ONE simultaneous pass (sequential renames collide on the dedupe key). No URL authored anywhere except per the T003 policy block.

## Next Actions (remainder of Session A — waves 4–6 of 14)

1. **Wave A4 / T005** (senior-backend-engineer): catalog surgery `schemas/taxonomy/owasp.yaml:439–517` per data-model.md §2 slot table — `full_id` → `OWASP-LLM-2026-NN`, verbatim 2026 names (LLM08 = "Hidden Context Exposure"), `url` = interim anchor per T003 policy, `# citation:` re-attested to the category now in each slot, `cwe_refs` per D6 fence (default `[]`); `id`/order/`out_of_scope` untouched. Then integrity suite green.
2. **Wave A5 / T006** (senior-backend-engineer): crosswalk single-pass simultaneous σ-permutation of the 57 non-hold `source.id` values; verify 645 edges / 0 duplicates / primary floor ≥500 / exact per-id vector; integrity suite green. **P1 architect checkpoint after A5** (generic P0/P1/P2 table maps P1 to waves 3–5; the σ oracles are the review evidence).
3. **Wave A6 / T007** (security-analyst, [MANUAL-ONLY], ~0.9d — the densest single task): 74-edge human disposition + 54 re-anchors per T003 policy; complete ledger 74/74. Then the **A6→B phase gate** (integrity green + ledger 74/74) blocks all user-story work until met.
4. Session B afterwards (new conversation): B1 = T008 ∥ T009 ∥ T011 ∥ T018, B2 = T010 ∥ T012 ∥ T013.

## Context Files

- `specs/362-remap-owasp-llm-top10-2026/tasks.md` — task text + dependencies (4/26 checked)
- `specs/362-remap-owasp-llm-top10-2026/agent-assignments.md` — wave map, gates, escalation triggers
- `specs/362-remap-owasp-llm-top10-2026/data-model.md` — §1 σ map, §2 slot table (T005/T006 normative input)
- `specs/362-remap-owasp-llm-top10-2026/crosswalk-disposition-ledger.md` — URL policy + σ-oracle headers + 74 pending rows
- `specs/362-remap-owasp-llm-top10-2026/test-results-prestate.md` — T024 reconciliation base + P0 ruling
- `specs/362-remap-owasp-llm-top10-2026/quickstart.md` — verification battery command forms

## Resume Command

```bash
claude "Resume F-362 implementation (branch: 362-remap-owasp-llm-top10-2026). Waves 1-3 complete (T001-T004; P0 GO; URL policy Branch 2). Run /aod.build to continue with Wave A4 (T005 catalog surgery). Read specs/362-remap-owasp-llm-top10-2026/NEXT-SESSION.md first — pre-flight warning about sibling-session examples/** PNGs applies."
```
