# T001 — Pre-State Test Capture (F-362: Remap OWASP LLM Top 10 → 2026)

**Capture date**: 2026-08-09
**HEAD SHA**: `033644224239fff2418ae8de679ce092b8ac5e03`
**Branch**: `362-remap-owasp-llm-top10-2026`
**Executor**: tester agent, T001 (verification-only — nothing fixed; nothing edited except this file and `.aod/results/tester.md`, per the KB 15 attribution rule)
**Environment**: Python 3.12.11, pytest 9.1.0, pluggy 1.6.0, plugins `cov-7.1.0` + `timeout-2.4.0`, rootdir configfile `pyproject.toml`, Typst 0.14.2

Runbook source: `specs/362-remap-owasp-llm-top10-2026/quickstart.md` §0. Gate source: `plan.md` §Verification & Gates item 1 ("Pre-state artifact (W0): literal totals checked into `specs/362-*/test-results-prestate.md`").

---

## Environmental note — concurrent sibling sessions (read before interpreting results below)

At capture time `ps aux` showed 6 other active Claude Code processes against this same repo checkout (PIDs 50992/51480/51482/54350/54398/54800/55044; several `--resume=<session-id>`; spans 2:13 PM–3:34 PM), independent of this T001 execution. Two categories of unrelated working-tree drift were observed in `git status --short` (37 files) during this task and are **not attributable to any command T001 ran**:

- `docs/product/_backlog/BACKLOG.md` — auto-regenerated timestamp only (`2026-08-09T18:26:14Z` → `2026-08-09T19:36:26Z`). Same disclaimed pattern as the F-295 T002 pre-state precedent (`/aod.status` side effect from some other lifecycle command, unrelated to any pytest invocation).
- 34 files under `examples/{agentic-app,maestro-reference,mermaid-agentic-app}/**/attack-{chains,trees}/*.png` — regenerated by another in-flight session's report-generation pipeline. **Verified not caused by this task**: grepped every module this task executed (all 15 gated modules + `test_backward_compatibility.py` + `test_taxonomy_integrity.py`) for `attack-tree`/`attack-chain` references — zero hits. The dirty-file count was stable at 37 both before and after the ~12-minute broad-census run (no growth), indicating the concurrent activity had already completed rather than racing against these test runs.
- **Isolation check for the 19 non-backward-compat out-of-gate failures in §4**: none of `threats.md`, the coverage-attestation fixtures, or the mobile/ml/llm10 enrichment source files that those failing tests read appear anywhere in the dirty-file set. Those 19 failures are read against a clean, stable tree — not artifacts of the concurrent sessions' writes.

The working tree was **not** restored to clean by this task — out of scope (`you FIX NOTHING`; reverting another session's in-progress output would itself be an edit beyond the two named deliverables). T024 should independently re-verify tree cleanliness at that time.

---

## 1. `test_backward_compatibility.py` (byte-identity suite)

**Command**: `python -m pytest tests/scripts/test_backward_compatibility.py -q`

**Literal summary line**:
```
6 failed, 7 passed, 1 skipped in 18.33s
```

**Failures** (all 6 share one failure signature — a font-subsetting tag divergence, not row/content drift):
```
FAILED tests/scripts/test_backward_compatibility.py::test_unmodified_examples_byte_identical_pdfs[web-app]
FAILED tests/scripts/test_backward_compatibility.py::test_unmodified_examples_byte_identical_pdfs[microservices]
FAILED tests/scripts/test_backward_compatibility.py::test_unmodified_examples_byte_identical_pdfs[ascii-web-api]
FAILED tests/scripts/test_backward_compatibility.py::test_unmodified_examples_byte_identical_pdfs[mermaid-agentic-app]
FAILED tests/scripts/test_backward_compatibility.py::test_unmodified_examples_byte_identical_pdfs[free-text-microservice]
FAILED tests/scripts/test_backward_compatibility.py::test_unmodified_examples_byte_identical_pdfs[maestro-reference]
```

Representative divergence (`web-app`, byte offset 224224): baseline embeds font subset tag `/ZZMJWG+.SFNS-Regu`; generated embeds `/SPUEUJ+.SFNS-Regu`. `SOURCE_DATE_EPOCH=1700000000` IS honored (the divergence sits in the font subset tag, not the PDF Info dictionary or XMP stream — the test's own error message explicitly rules out the "env var not honored" hypothesis for that case). Re-ran the `web-app` case standalone as a stability check: **identical tag (`SPUEUJ+`) and identical byte offset (224224) on the second invocation** — the divergence is deterministic-per-environment, not per-run-random. This is consistent with a local Typst 0.14.2 font-subsetting behavior differing from whatever toolchain produced the committed baselines, i.e. **environmental/toolchain drift, not repo content drift** — and clearly unrelated to F-362's taxonomy-ID remap scope (F-362 touches no Typst templates, fonts, or rendering code).

**Flag for whoever reconciles at T024**: this is a *different* failure signature than prior pre-state captures on this file — F-185's pre-state (2026-06-11) was ATLAS 30→36 row-count content drift (since fixed); F-295's pre-state (2026-07-03) was fully green (13 passed, 1 skipped). This font-subset-tag signature is new as of this capture. `plan.md` §Verification & Gates item 3 requires this suite green pre-merge with zero baseline bytes changed — since F-362 does not touch rendering/fonts, this red state is not F-362's to fix, but it will block the byte-identity gate at T024 unless resolved by an orthogonal toolchain/baseline-regen action before then. Recording as-is per KB 15; not investigated further (out of scope for T001).

**Skip** (verbatim):
```
SKIPPED [1] tests/scripts/test_backward_compatibility.py:392: mermaid-agentic-app is excluded from SC-003 per T033 narrowed interpretation (multi-agent gate predicate evaluates TRUE via condition (a)+(b); pattern classification is a documented known-limitation pending R-04/R-06 rule-tuning follow-up).
```

---

## 2. `test_taxonomy_integrity.py`

**Command**: `python -m pytest tests/schemas/test_taxonomy_integrity.py -q`

**Literal summary line**:
```
5 passed in 0.42s
```

All green — no pre-existing red state in the taxonomy integrity suite.

---

## 3. Gated pytest subset (`.github/workflows/tachi-pytest.yml`, 15-module)

**Exact invocation** (copied verbatim from the workflow's "Run F-248 + F-256 hardening pytest suite" step):

```bash
python -m pytest \
  tests/scripts/test_init_sh_substitution.py \
  tests/scripts/test_init_sh_adversarial.py \
  tests/scripts/test_init_sh_constitution.py \
  tests/scripts/test_init_sh_self_delete.py \
  tests/scripts/test_template_substitute_unit.py \
  tests/scripts/test_init_input_unit.py \
  tests/scripts/test_substitute_shim_canary.py \
  tests/scripts/test_init_sh_defaults_env.py \
  tests/scripts/test_template_config_load_unit.py \
  tests/scripts/test_template_config_load_integration.py \
  tests/scripts/test_template_git_clone_timeout.py \
  tests/scripts/test_template_substitute_lint_no_eval.py \
  tests/scripts/test_init_precommit_matrix.py \
  tests/scripts/test_asset_sensitivity_tags.py \
  tests/scripts/test_affected_assets_wiring.py \
  -v --timeout=1080
```

**Literal summary line**:
```
============ 147 passed, 1 skipped, 1 xfailed in 695.26s (0:11:35) =============
```

**Skip** (verbatim):
```
SKIPPED [1] tests/scripts/test_init_precommit_matrix.py:164: Case 4 (TTY × no-flag default-Y) requires real TTY simulation via pty.openpty(); the multi-prompt personalization sequence makes pty input feeding fragile. T017 covers this scenario via manual empirical verification (5-scenario walkthrough). Re-enable if pty harness lands.
```

**XFAIL** (verbatim — see §5 for the full #345 discussion):
```
XFAIL tests/scripts/test_init_sh_substitution.py::test_personalized_tree_bytes_match_baseline - Pre-existing baseline-fixture staleness — this test was already red at the v4.44.0 release (6b3481e), BEFORE the #338 F-248/F-256 hardening clobber. The init-baseline-tree fixture has drifted from current substitution-target sources for reasons unrelated to substitution behavior (the F-248/F-256 hardening tests pass). Tracked for fixture regen under #345 (re-tagged from #329 per OQ-6 — #329 is the orthogonal ORDERED_FRAMEWORKS catalog-drift guard over the CA-PDF baselines, NOT the init.sh fixture surface). strict=False so a future regen that fixes it surfaces as XPASS — the signal to delete this marker.
```

**Zero failures** in the gated subset — this is the CI-enforced gate and it is green today.

---

## 4. Broader census (`tests/scripts/ tests/schemas/ -q`)

**Command**: `python -m pytest tests/scripts/ tests/schemas/ -q`

**Literal summary line**:
```
25 failed, 943 passed, 4 skipped, 1 xfailed, 5 warnings in 728.02s (0:12:08)
```

**Internal consistency check** (broad census totals reconcile exactly against the three narrower runs above):
- Passed: 147 (gated-15, §3) + 7 (`test_backward_compatibility.py`, §1) + 789 (everything else, incl. the 5 in `test_taxonomy_integrity.py`, §2) = 943 ✓
- Skipped: 1 (gated-15 `test_init_precommit_matrix.py`, §3) + 1 (`test_backward_compatibility.py` mermaid-agentic-app, §1) + 2 (`test_maestro_coverage_invariant.py` — not exercised by §1–§3, see below) = 4 ✓
- Xfailed: 1 (#345, identical to §3) ✓ · Xpassed: 0 (both runs)
- Failed: 6 (`test_backward_compatibility.py`, §1) + 19 (other out-of-gate modules, table below) = 25 ✓

### Out-of-gate failure census (25 failures across 8 modules — none in the 15-module gate)

| Module | Failing tests | Count | In gate? |
|---|---|---|---|
| `test_backward_compatibility.py` | byte-identity ×6 (see §1) | 6 | NO — captured separately in §1 |
| `test_coverage_attestation_audit.py` | `test_every_covered_owasp_has_agent_citation`, `test_every_covered_owasp_has_pattern_category_citation` | 2 | NO |
| `test_coverage_attestation_in_scope.py` | `test_aggregator_matches_expected_fixture[findings_in_scope_only]`, `[findings_oos_only]`, `[findings_mixed]` | 3 | NO |
| `test_llm10_unbounded_consumption_enrichment.py` | `test_dos_agent_line_count_within_cap`, `test_model_theft_agent_line_count_within_cap` | 2 | NO |
| `test_ml_top_10_coverage_bundle_enrichment.py` | `test_tampering_md_line_cap`, `test_model_theft_md_line_cap` | 2 | NO |
| `test_mobile_top_10_coverage_bundle_enrichment.py` | `test_spoofing_md_line_cap`, `test_tampering_md_line_cap`, `test_info_disclosure_md_line_cap`, `test_privilege_escalation_md_line_cap`, `test_repudiation_md_line_cap`, `test_privilege_escalation_companion_has_M8_priv_gain` | 6 | NO |
| `test_pyyaml_deferred_import.py` | `test_yaml_import_is_function_scoped[check-citation-urls.py]` | 1 | NO |
| `test_tool_abuse_enrichment.py` | `test_tool_abuse_line_count_within_cap`, `test_categories_1_8_byte_identity_against_main`, `test_validate_source_attribution_on_regen` | 3 | NO |
| **Total** | | **25** | all NO |

**Reconciliation with the "~19 known out-of-gate failures" prior estimate** (task framing / project memory): 25 total − 6 `test_backward_compatibility.py` (tracked separately, §1, with its own quickstart.md step) = **19** — an exact match. The prior "~19" estimate refers specifically to the non-backward-compat out-of-gate set (7 modules); none of these 7 modules or their failing tests touch F-362's taxonomy-remap surface (`schemas/taxonomy/*.yaml`, coverage docs, agent files) — the near-misses (`LineCountCaps`, `TestCitationCompleteness`, byte-identity-against-main, source-attribution-on-regen) are pre-existing content-cap/citation-fixture drift orthogonal to an OWASP-edition-label remap.

**Skips beyond §1/§3** (verbatim, both from `test_maestro_coverage_invariant.py`, a module not exercised by any narrower run above):
```
SKIPPED [1] tests/scripts/test_maestro_coverage_invariant.py:140: examples/consumer-agent-app/sample-report/threats.md has no 'Risk by MAESTRO Layer' table (intermediate-format sample report); coverage invariant N/A.
SKIPPED [1] tests/scripts/test_maestro_coverage_invariant.py:140: examples/predictive-ml-app/sample-report/threats.md has no 'Risk by MAESTRO Layer' table (intermediate-format sample report); coverage invariant N/A.
```

**Warnings** (5×, one class, non-blocking):
```
PytestRemovedIn10Warning: Class-scoped fixture defined as instance method is deprecated.
```
Raised across 5 parametrized cases in `test_finding_pattern_parser.py` (fixture-definition style deprecation, not an assertion failure).

---

## 5. xfail/xpass census — issue #345

**Confirmed PRESENT**, identically in both the gated-15 run (§3) and the broader census (§4):

- **Test**: `tests/scripts/test_init_sh_substitution.py::test_personalized_tree_bytes_match_baseline`
- **Marker**: `@pytest.mark.xfail(strict=False)` — source: `tests/scripts/test_init_sh_substitution.py:35-48`
- **Reason** (from the marker, verbatim): "Pre-existing baseline-fixture staleness — this test was already red at the v4.44.0 release (6b3481e), BEFORE the #338 F-248/F-256 hardening clobber. ... Tracked for fixture regen under #345 ... strict=False so a future regen that fixes it surfaces as XPASS — the signal to delete this marker."
- **Result across every run performed today**: XFAIL (never XPASS) — the underlying fixture-staleness bug remains unresolved as of this capture.
- **xpassed count**: 0 (in every run — gated-15 §3 and broad census §4)

No other `xfail` markers were encountered in any of the four runs performed for this capture.

---

## Reconciliation base (for T024)

These are the T024 comparison baseline totals. Per `tasks.md` T024, deltas at T024 are expected **only** from the declared T017 (net-new `tests/scripts/test_owasp_2026_contract.py`, joining the gated subset — 15→16 modules) and T020 (new function added inside the already-out-of-gate-relative-to-`tachi-pytest.yml` `test_catalog_drift_guard.py`, gated instead by `tachi-catalog-drift.yml`) test additions, both with commit-message-declared expected count deltas (team-lead F5).

| Metric | T001 pre-state value | Expected at T024 |
|---|---|---|
| Gated 15-module subset (§3) | 147 passed, 1 skipped, 1 xfailed, 0 failed | 15→16 modules once T017 lands; passed count rises by T017's declared new-test count; skip/xfail/fail otherwise unchanged |
| `test_backward_compatibility.py` (§1) | 6 failed, 7 passed, 1 skipped | Green (0 failed) required by FR-007a/FR-010 no-churn proof — **but see the §1 flag**: today's red is a toolchain/font-subsetting signature orthogonal to F-362, not something F-362's own commits would flip; if still red at T024 for the identical `ZZMJWG+`/`SPUEUJ+` signature, that is pre-existing environmental drift, not an F-362 regression |
| `test_taxonomy_integrity.py` (§2) | 5 passed | Unchanged (5 passed) — re-run after W1 and again at W5 per quickstart.md §1 |
| Broad-census non-backward-compat out-of-gate failures (§4) | 19 across 7 modules | Unchanged — none of these 7 modules are in F-362's edit surface; any delta here at T024 is a regression, not an expected change |
| xfail/xpass, issue #345 (§5) | 1 xfail, 0 xpass | **Unchanged** — team-lead F5 / architect MEDIUM-2 explicitly: "#345's xfail persists" at T024 |

**Attribution rule (KB 15)**: any red/green flip observed at T024 that is NOT one of (a) the T017 gated-count rise, (b) the T020 in-module test-count rise inside `test_catalog_drift_guard.py`, or (c) the `test_backward_compatibility.py` byte-identity gate turning green via an explicit, F-362-scoped no-churn proof, is **not attributable to F-362** and should be triaged as either environmental (like the font-subsetting signature above) or a genuine regression requiring investigation — not silently absorbed into this feature's delivery.
